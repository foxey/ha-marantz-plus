# Describes the format for available marantzplus services

get_command:
  target:
    entity:
      integration: marantzplus
      domain: media_player
  fields:
    command:
      example: "/goform/formiPhoneAppDirect.xml?RCKSK0410370"
      required: true
      selector:
        text:
set_dynamic_eq:
  target:
    entity:
      integration: marantzplus
      domain: media_player
  fields:
    dynamic_eq:
      default: true
      selector:
        boolean:
update_audyssey:
  target:
    entity:
      integration: marantzplus
      domain: media_player
